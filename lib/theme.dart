import 'package:flutter/material.dart';

final myDarkTheme = ThemeData.dark().copyWith(
  accentColor: Colors.amber,
);
final myLightTheme =
    ThemeData.light().copyWith(iconTheme: IconThemeData(color: Colors.white10));
